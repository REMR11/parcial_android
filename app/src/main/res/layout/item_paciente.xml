<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="2dp"
    app:strokeWidth="0dp"
    app:cardBackgroundColor="@color/card_background">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="8dp">

            <!-- Paciente Name -->
            <TextView
                android:id="@+id/tvPacienteNombre"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Juan Pérez Gavidia"
                android:textColor="@color/text_primary"
                android:textSize="17sp"
                android:textStyle="bold"
                android:layout_alignParentStart="true"
                android:layout_toStartOf="@id/cardPriorityBadge"
                android:layout_marginEnd="12dp"
                android:maxLines="1"
                android:ellipsize="end" />

            <!-- Priority Badge Container -->
            <com.google.android.material.card.MaterialCardView
                android:id="@+id/cardPriorityBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                app:cardCornerRadius="6dp"
                app:strokeWidth="0dp"
                app:cardBackgroundColor="@color/priority_medium_bg">
                
                <TextView
                    android:id="@+id/tvPacientePrioridad"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="MEDIA"
                    android:textColor="@color/priority_medium_text"
                    android:textSize="11sp"
                    android:textStyle="bold"
                    android:paddingHorizontal="8dp"
                    android:paddingVertical="4dp" />
            </com.google.android.material.card.MaterialCardView>

        </RelativeLayout>

        <!-- Subtitles: Edad and DUI -->
        <TextView
            android:id="@+id/tvPacienteInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Edad: 35 años  •  DUI: 01234567-8"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:layout_marginBottom="4dp" />

        <!-- Date of Registration -->
        <TextView
            android:id="@+id/tvPacienteFecha"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Registrado: 22/05/2026 07:00 AM"
            android:textColor="@color/text_secondary"
            android:textSize="11sp"
            android:layout_marginBottom="12dp" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/divider"
            android:layout_marginBottom="10dp" />

        <!-- Row of Action Buttons -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="end">

            <!-- Ver Detalle -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnItemDetalle"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Ver Detalle"
                android:textSize="12sp"
                style="@style/Widget.Material3.Button.TextButton"
                android:textColor="@color/primary"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:layout_marginEnd="8dp" />

            <!-- Editar -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnItemEditar"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Editar"
                android:textSize="12sp"
                style="@style/Widget.Material3.Button.TextButton"
                android:textColor="@color/priority_medium_text"
                android:insetTop="0dp"
                android:insetBottom="0dp"
                android:layout_marginEnd="8dp" />

            <!-- Eliminar -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnItemEliminar"
                android:layout_width="wrap_content"
                android:layout_height="36dp"
                android:text="Eliminar"
                android:textSize="12sp"
                style="@style/Widget.Material3.Button.TextButton"
                android:textColor="@color/priority_high_text"
                android:insetTop="0dp"
                android:insetBottom="0dp" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
